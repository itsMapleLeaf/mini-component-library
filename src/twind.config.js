// @ts-check
import { COLORS } from "./constants"

const config = {
  theme: {
    colors: COLORS,
    boxShadow: {
      inset: `inset 0px 2px 4px rgba(128, 128, 128, 0.35);`,
    },
  },
}

export default config
